services:
  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile.api
    ports:
      - "8000:8000"
    environment:
      DEVICE: cpu
      CORS_ORIGINS: http://localhost:5173,http://127.0.0.1:5173
    volumes:
      - ../weights:/app/weights
      - ../audit:/app/audit
